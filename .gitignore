.venv/
__pycache__/
*.pyc
.DS_Store
.env
.env.*
.streamlit/secrets.toml

# data/*.parquet is tracked on purpose: Streamlit Cloud has no persistent disk,
# so GitHub Actions commits the shared market-data cache into the repo.
